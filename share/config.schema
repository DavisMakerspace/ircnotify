module IRC
  SERVER ||= ""
  PORT ||= 6667
  NICK ||= "ircnotify"
  CHANNELS ||= []
  CMDPREFIX ||= "!"
  MSGFORMAT ||= "%{src} :: %{msg}"
end
module Server
  PATH ||= "/run/ircnotify/#{IRC::NICK}"
  URLSHORTEN ||= true
  URLMAXLEN ||= 20
end
