module IRC
  SERVER ||= ""
  PORT ||= 6667
  NICK ||= "ircnotify"
  CHANNELS ||= []
  CMDPREFIX ||= "!"
  MSGFORMAT ||= "%{src} :: %{msg}"
  MESG_PER_SECOND ||= 0.7
end
module Server
  PATH ||= "/run/ircnotify/#{IRC::NICK}"
  MODE ||= nil
  URLSHORTEN ||= false
  URLMAXLEN ||= 20
end
